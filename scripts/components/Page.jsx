'use strict';

var React = require('react'),

    Page = React.createClass({
        render: function () {
            return (
                <div className="container">

                </div>
            )
        }
    });
module.exports = Page;